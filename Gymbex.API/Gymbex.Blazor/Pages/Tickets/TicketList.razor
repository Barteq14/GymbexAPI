@page "/tickets"
@using Gymbex.Blazor.Components

<div class="content-container">
    <div class="alerts-box">
        <AlertBlazor Class="@(IsError ? "alert alert-danger" : "alert alert-success")"
                     IsError="IsError"
                     Content="@Content" />
    </div>

    <BlazorList Items="TicketsList" Title="Dostępne karnety" />
</div>

